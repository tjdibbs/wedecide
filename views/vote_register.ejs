<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>WeDecide</title>
    <!-- inject:css -->
    <link rel="stylesheet" href="/voters/css/vertical-layout-light/style.css" />
    <!-- endinject -->
  </head>

  <body>
    <div class="container-scroller">
      <div class="container-fluid page-body-wrapper full-page-wrapper">
        <div class="content-wrapper d-flex align-items-center auth px-0">
          <div class="row w-100 mx-0">
            <div class="col-lg-4 mx-auto">
              <div class="auth-form-light text-left py-5 px-4 px-sm-5">
                <h4>New here?</h4>
                <h6 class="font-weight-light">
                  Sign up to vote, it is easy. It only takes a few steps
                </h6>
                <form
                  action="/voter/register-election"
                  enctype="multipart/form-data"
                  method="POST"
                  class="pt-3"
                >
                  <%- include("message") -%>
                  <div class="form-group">
                    <select
                      name="election"
                      class="form-control form-control-lg"
                      id="exampleFormControlSelect2"
                    >
                      <% for( let index=0; index < result.length; index++ ) { %>
                      <option
                        value="<%= result[index]._id %>/<%= result[index].name %>"
                      >
                        <%= result[index].name %>
                      </option>
                      <% } %>
                    </select>
                  </div>
                  <div class="form-group">
                    <input required name="username" type="text" value="<%=
                    formData?.username ?? "" %>" class="form-control
                    form-control-lg" id="exampleInputUsername1"
                    placeholder="Username">
                  </div>
                  <div class="form-group">
                    <input required name="email" type="email" value="<%=
                    formData?.email ?? "" %>" class="form-control
                    form-control-lg" id="exampleInputEmail1"
                    placeholder="Email">
                  </div>
                  <div class="form-group">
                    <input required name="phone" type="number" value="<%=
                    formData?.phone ?? "" %>" class="form-control
                    form-control-lg" id="exampleInputPhone" placeholder="Phone">
                  </div>
                  <div class="form-group">
                    <input required name="password" type="password" value="<%=
                    formData?.password ?? "" %>" class="form-control
                    form-control-lg" id="exampleInputPassword1"
                    placeholder="Password">
                  </div>

                  <div class="form-group">
                    <label>Upload Facial Photo</label>
                    <div class="input-group col-xs-12">
                      <input
                        required
                        type="text"
                        name="image_name"
                        class="form-control file-upload-info"
                        placeholder="Upload Image"
                      />
                      <span class="input-group-append">
                        <button
                          class="file-upload-browse btn btn-primary"
                          type="button"
                        >
                          Upload
                        </button>
                      </span>
                    </div>
                    <input
                      type="file"
                      name="image"
                      accept="image/*"
                      capture="camera"
                      class="file-upload-default"
                    />
                  </div>

                  <div class="mt-3">
                    <button
                      type="submit"
                      class="btn btn-block btn-primary btn-lg font-weight-medium auth-form-btn"
                    >
                      SIGN UP
                    </button>
                  </div>
                  <div class="text-center mt-4 font-weight-light">
                    Already paid for an election?
                    <a href="/voter/vote-election" class="text-primary"
                      >Login</a
                    >
                  </div>
                </form>
              </div>
            </div>
          </div>
        </div>
        <!-- content-wrapper ends -->
      </div>
      <!-- page-body-wrapper ends -->
    </div>
    <!-- container-scroller -->
    <!-- plugins:js -->
    <script src="/voters/vendors/js/vendor.bundle.base.js"></script>
    <!-- endinject -->
    <!-- inject:js -->
    <script src="/voters/js/file-upload.js"></script>
  </body>
</html>

<!DOCTYPE html>
<html lang="en">

<head>
  <!-- Required meta tags -->
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <title>WeDecide</title>

  <link rel="stylesheet" href="/voters/css/vertical-layout-light/style.css" />
  <!-- endinject -->
</head>

<body>
  <div class="container-scroller">
    <div class="container-fluid page-body-wrapper full-page-wrapper">
      <div class="content-wrapper d-flex align-items-center auth px-0">
        <div class="row w-100 mx-0">
          <div class="col-lg-4 mx-auto">
            <div class="auth-form-light text-left py-5 px-4 px-sm-5">
              <h4>New here?</h4>
              <h6 class="font-weight-light">
                Sign up to vote, it is easy. It only takes a few steps
              </h6>
              <form action="/voter/contest-register" method="POST" class="pt-3">
                <%- include("message") %>
                  <div class="form-group">
                    <select name="contest" class="form-control form-control-lg" id="exampleFormControlSelect2">

                      <% for( let app=0; app < result.length; app++ ) { %>
                        <option selected="<%= formData?.contest?.split(" /")[1]===result[app].name%>" value="<%=
                            result[app]._id %>/<%= result[app].name %>">
                              <%= result[app].name %>
                        </option>
                        <% } %>
                    </select>
                  </div>

                  <div class="form-group">
                    <input required name="name" type="text" class="form-control form-control-lg"
                      id="exampleInputUsername1" placeholder="Name" value="<%= formData?.name ?? "" %>" />
                  </div>
                  <div class="form-group">
                    <input required name="email" type="email" value="<%= formData?.email ?? "" %>"
                      class="form-control form-control-lg" id="exampleInputEmail1" placeholder="Email" />
                  </div>
                  <div class="form-group">
                    <input required name="phone" type="number" value="<%= formData?.phone ?? "" %>"
                      class="form-control form-control-lg" id="exampleInputPhone" placeholder="Phone" />
                  </div>
                  <div class="mt-3">
                    <button type="submit" class="btn btn-block btn-primary btn-lg font-weight-medium auth-form-btn">
                      SIGN UP
                    </button>
                  </div>
                  <div class="text-center mt-4 font-weight-light">
                    Already paid for an election?
                    <a href="/voter/contest-vote" class="text-primary">
                      I Have A Vouchar
                    </a>
                  </div>
              </form>
            </div>
          </div>
        </div>
      </div>
      <!-- content-wrapper ends -->
    </div>
    <!-- page-body-wrapper ends -->
    <script src="/vendors/js/vendor.bundle.base.js"></script>
    <script src="/js/bootstrap.min.js"></script>
  </div>
  <!-- container-scroller -->
</body>

</html>